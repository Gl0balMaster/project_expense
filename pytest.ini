[pytest]
DJANGO_SETTINGS_MODULE = expense_tracker.settings
python_files = test_*.py *_tests.py
addopts = 
    --tb=short
    --strict-markers
    -v
testpaths = expenses/tests
markers =
    slow: slow running tests
    integration: integration tests
    db: database tests